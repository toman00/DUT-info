/* premier programme */

#include <stdlib.h>
#include <stdio.h>

int main(void) {
  printf("%d si la sortie est bonne. %d si la variable de sortie est mauvaise.",EXIT_SUCCESS,EXIT_FAILURE);
  return EXIT_SUCCESS;
}

